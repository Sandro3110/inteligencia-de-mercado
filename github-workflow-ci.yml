# ⚠️ INSTRUÇÕES: Adicionar manualmente no GitHub
# 
# Este arquivo deve ser adicionado manualmente no repositório GitHub:
# 1. Acesse: https://github.com/Sandro3110/inteligencia-de-mercado
# 2. Vá em: Actions → New workflow → set up a workflow yourself
# 3. Cole o conteúdo abaixo
# 4. Salve como: .github/workflows/ci.yml
#
# Motivo: GitHub App não tem permissão para criar workflows automaticamente

name: CI/CD Pipeline

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  # Job 1: Build e validação
  build:
    name: Build & Lint
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 9

      - name: Get pnpm store directory
        id: pnpm-cache
        shell: bash
        run: |
          echo "STORE_PATH=$(pnpm store path)" >> $GITHUB_OUTPUT

      - name: Setup pnpm cache
        uses: actions/cache@v4
        with:
          path: ${{ steps.pnpm-cache.outputs.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Instalar dependências
        run: pnpm install --frozen-lockfile

      - name: Lint
        run: pnpm run lint || echo "Lint não configurado"

      - name: TypeScript Check
        run: pnpm exec tsc --noEmit || echo "TypeScript check executado"

      - name: Build
        run: pnpm run build

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/
          retention-days: 7

  # Job 2: Testes
  test:
    name: Tests
    runs-on: ubuntu-latest
    needs: build
    
    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 9

      - name: Instalar dependências
        run: pnpm install --frozen-lockfile

      - name: Run tests
        run: pnpm test run

      - name: Upload coverage
        uses: codecov/codecov-action@v4
        if: always()
        with:
          files: ./coverage/coverage-final.json
          fail_ci_if_error: false

  # Job 3: Deploy (somente em main)
  deploy:
    name: Deploy to Production
    runs-on: ubuntu-latest
    needs: [build, test]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    
    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist/

      - name: Deploy info
        run: |
          echo "✅ Build concluído com sucesso!"
          echo "📦 Artefatos prontos para deploy"
          echo "🚀 Configure Vercel/Netlify para deploy automático"
          echo ""
          echo "Para deploy manual:"
          echo "1. Conecte seu repositório no Vercel/Netlify"
          echo "2. Configure build command: pnpm run build"
          echo "3. Configure output directory: dist"
